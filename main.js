
//make sure that input box has white background if empty
const input = document.getElementById('input-box');

let checkContent = () => {
    if(input.value === "" && input.value === null ){
        input.style.background
    }
}